name: PR Agent
on:
  pull_request:
  issue_comment:
permissions:
  contents: read
  pull-requests: write
  issues: write
jobs:
  pr-agent:
    uses: axiomantic/.github/.github/workflows/pr-agent.yml@devel
    secrets:
      OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
