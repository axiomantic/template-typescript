/**
 * Greet someone.
 *
 * @example
 * ```ts
 * import { hello } from "@axiomantic/project-name";
 * hello("world"); // "Hello, world!"
 * ```
 */
export function hello(name: string): string {
  return `Hello, ${name}!`;
}
